class Player extends Phaser.GameObjects.Image
{
   // INSTANCE FIELDS
   //spriteScale = 4;
   //flipped = false;

   // CONSTRUCTOR
   constructor(scene)
   {
      super(30, 30, "player");
      //this.physics.add.image(config.width/2-50, config.height/2+50, "player");
      //this.setScale(4);
      //this.flipped = false;
   }
}